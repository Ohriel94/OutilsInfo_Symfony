{# templates/home.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
    <div {{ react_component('Hello', { fullName: "Barran Orba" }) }}>
        Loading... <i class="fas fa-cog fa-spin fa-3x"></i>
    </div>
    <div>
        {{ react_component('Hello', { fullName: "Barran Orba" }) }}
    </div>
{% endblock %}